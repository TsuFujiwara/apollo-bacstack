import React, { Fragment } from "react";
import { BacnetTargetsTable } from "../components";

const Targets = ({ classes }) => {
  return (
    <Fragment>
      <BacnetTargetsTable />
    </Fragment>
  );
};

export default Targets;
